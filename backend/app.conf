{
  # Mojolicious Konfiguration
  hypnotize => {
    listen => ['http://*:3001'],
  },
  
  # Logging
  log => {
    level => 'info',
    path => 'log/xbillr.log',
  },
  
  # Datenbank (kann durch Umgebungsvariablen überschrieben werden)
  database => {
    dsn => 'dbi:MariaDB:database=xbillr;host=localhost;port=3307',
    user => 'xbillr_user',
    password => 'xbillr_pass',
  },
}

