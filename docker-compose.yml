services:
  mariadb:
    image: mariadb:10.11
    container_name: xbillr-db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-u9UUgy2ZwASrTebZ8pAGaCPnVSJZ8NRX}
      MYSQL_DATABASE: xbillr
      MYSQL_USER: ${DB_USER:-root}
      MYSQL_PASSWORD: ${DB_PASSWORD:-u9UUgy2ZwASrTebZ8pAGaCPnVSJZ8NRX}
    ports:
      - "${DB_PORT:-3397}:3306"
    volumes:
      - mariadb_data:/var/lib/mysql
      - ./backend/sql/schema.sql:/docker-entrypoint-initdb.d/01-schema.sql:ro
      - ./backend/sql/user_management.sql:/docker-entrypoint-initdb.d/02-user_management.sql:ro
    command: >
      --character-set-server=utf8mb4
      --collation-server=utf8mb4_unicode_ci
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: xbillr-backend
    restart: unless-stopped
    depends_on:
      mariadb:
        condition: service_healthy
    environment:
      DB_DSN: "dbi:MariaDB:database=xbillr;host=mariadb;port=3306"
      DB_USER: ${DB_USER:-root}
      DB_PASSWORD: ${DB_PASSWORD:-u9UUgy2ZwASrTebZ8pAGaCPnVSJZ8NRX}
      ALLOWED_ORIGINS: ${ALLOWED_ORIGINS:-http://localhost:3002,http://localhost:8080,http://127.0.0.1:3002,http://127.0.0.1:8080,file://}
    ports:
      - "${BACKEND_PORT:-3002}:3002"
    volumes:
      - ./backend/log:/app/log
      - ./config/xbillr.conf:/app/config/xbillr.conf:ro
    healthcheck:
      test: ["CMD", "perl", "-e", "use LWP::Simple; exit(get('http://127.0.0.1:3002/api/health') ? 0 : 1)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

volumes:
  mariadb_data:
    driver: local

